name: Build and Push Backend to ACR

on:
  push:
    branches:
      - main # Tự động kích hoạt khi có code mới đẩy lên nhánh main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Login to Azure Container Registry
        uses: azure/docker-login@v1
        with:
          login-server: opshubregistry.azurecr.io
          username: ${{ secrets.ACR_USERNAME }}
          password: ${{ secrets.ACR_PASSWORD }}

      - name: Build and Push Docker Image
        run: |
          cd backend # Đổi thành tên thư mục chứa backend của bạn
          docker build -t opshubregistry.azurecr.io/ops-backend:latest .
          docker push opshubregistry.azurecr.io/ops-backend:latest